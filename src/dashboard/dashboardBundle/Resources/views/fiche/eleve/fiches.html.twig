{% extends "dashboarddashboardBundle::baseEleves.html.twig" %}

{% block body %}
    <div id="wrapper">
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Toutes les fiches</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <h1>Fiches</h1>
        <div class="table-responsive">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                <tr>
                    <th>#</th>
                    <th>titre</th>
                    <th>status</th>

                    <th>note</th>

                </tr>
                </thead>
                <tbody>

                      {% for fiches in fiche %}
                      <tr>
                          <td>{{ loop.index }}</td>
                          <td><a href=""><a href="">{{ fiches.fiche.title | raw }}</a></a></td>
                    <td> {% if fiches.status == '1' %}
                            <a href="{{  path("student.fiches.make",{'id' : fiches.fiche.id}) }}"><button type="button" class="btn btn-success"><i class="fa fa-check"></i> Fait</button></a>
                        {% else %}
                            <a href="{{  path("student.fiches.make",{'id' : fiches.fiche.id}) }}"><button type="button" class="btn btn-warning"><i class="fa fa-bell"></i> À Faire</button></a>
                        {% endif %}</td>
                          <td>{{ fiches.note }}</td>
                      </tr>
                    {% endfor %}


                </tbody>
            </table>
        </div>
    </div>
{% endblock %}