{% extends "dashboarddashboardBundle::baseEleves.html.twig" %}

{% block body %}
    <div id="wrapper">
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Détail du Quizz</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            {% for flashMessage in app.session.flashbag.get('notice') %}
                <span class="alert alert-success">{{ flashMessage }}</span>
            {% endfor %}


            <!-- /.panel -->





                                    {{ score.fiche.title | raw }}

  {{ score.fiche.content | raw }}

   {{ form_start(form) }}



        {% for contacts in contact %}
       {{ contacts.contentChoice }}<br>


        {% endfor %}

 {% for key, formReponse in form.children %}


              <label class="mdl-radio mdl-js-radio mdl-js-ripple-effect mdl-cell mdl-cell--12-col" for="{{ formReponse.vars.id }}">
                {{ form_widget(formReponse, {'attr' : {'type': 'checkbox', 'name': formReponse.vars.full_name, 'value': formReponse.vars.value, 'id': formReponse.vars.id}}) }}
                <span class="mdl-radio__label">{{ formReponse.vars.label }}</span>
              </label>
     {{ form_widget(form) }}



   {% endfor %}


                <p>
                    <input type="submit" value="Valider" class="btn"/>
                </p>




                                   {{ form_end(form) }}



        </div>
    </div>
</div>




{% endblock %}